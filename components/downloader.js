

module.exports = function(element, grid, basis, pathsList) {
    element
}

function svgHref(svgString){
    button.setAttribute("download", "test.svg");    
    var url = "data:image/svg+xml;utf8," + svgString;
    button.setHref(url);
}